# development mode specific config properties
db.driverClassName=org.h2.Driver
db.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=false
db.username=sa
db.password=
hibernate.dialect=org.hibernate.dialect.MySQL5Dialect
# The following fixes the problem of Spring's inability of creating beans that implement interfaces
spring.aop.proxy-target-class=true